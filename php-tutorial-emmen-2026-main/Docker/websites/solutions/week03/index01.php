
<pre><code>
Assignment 1: explode() and implode()
<?php

  function myExplode(string $separator, string $text) : array {
      $result = [];

      $part = "";
      do {
          // find position of separator in string; if present hack the string at that position until empty
          // if not found: part = whole string.
      }while ($text !== "");

      return $result;
  }

  echo "Explode " . explode(", ","abc,def,g,e,rter,werwt4,wefrwer") . PHP_EOL;


?>
</code></pre>
