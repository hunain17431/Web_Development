#  Week 2 - HTML - Grid demo 2

A demo with four cards in a 2 by 2 grid. 

In this demo we use the `grid-template-columns` to define how the columns should behave. In this case we use a fixed
column of `25vw` (25% of the View Width) and a second column whose width is determined by the browser (`auto`).